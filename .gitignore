# dependencies
/node_modules

# next.js
/.next
/out
next-env.d.ts

# local databases (never commit raw data)
*.db
*.db-journal
*.db-wal
*.db-shm
# pre-SQLCipher plaintext backups left by `npm run db:encrypt`
*.db.plain
/data/

# environment
.env*

# test coverage
/coverage

# build / tooling artifacts
*.tsbuildinfo

# compiled OCR CLI (source lives in tools/ocr/main.swift; npm run build:ocr)
/tools/ocr/fairjudge-ocr

# macOS
.DS_Store
