// Tailwind CSS v4 is configured primarily via CSS (see src/app/globals.css).
// This file is loaded through the `@config` directive and holds the content
// globs plus a place to extend the theme in later milestones.
const config = {
  content: ["./src/**/*.{ts,tsx,mdx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
